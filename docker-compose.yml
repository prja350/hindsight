version: "3.9"
services:
  hindsight:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - ./data/db:/app/data/db
    environment:
      - FINMIND_TOKEN=${FINMIND_TOKEN:-}
      - CACHE_DIR=/app/data/db
      - PORT=8050
